[build]
  command = "npm run build"
  publish = "dist"

# Redirection SPA - toutes les routes vers index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"

